language: cpp
compiler: gcc
install:
  - sudo apt-get update
  - sudo apt-get install libqt4-dev qt4-qmake

script:
  - qmake qreal.pro
  - make

notifications:
  email:
    - qreal-build-reports@googlegroups.com
